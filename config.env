# Global configuration for dashboard bundle
# Edit these for your laptop + network

# Backend host (this machine's hostname or IP)
BACKEND_HOST=172.25.58.238
API_PORT=8081
NATS_PORT=4222

# Target Pi (hostname or IP)
PI_HOST=cncpi.local
PI_USER=pi
SSH_KEY=~/.ssh/id_raspberry_pi

# Edge DNC service
DNC_PORT=8083
HEIDENHAIN_SENDER=/home/pi/heidenhain_sender.py
MACHINE_ID=CNC-PI-001

# Frontend device id and mapping (frontend/.env.local will be generated)
DEVICE_ID=rpi-001
