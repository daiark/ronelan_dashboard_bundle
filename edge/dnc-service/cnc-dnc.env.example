# CNC DNC service environment (to be placed on the Pi at /etc/cnc-dnc.env)
# Adjust <> placeholders on site, then: sudo systemctl restart cnc-dnc.service

# Where DNC programs are stored on the Pi
DNC_PROGRAM_DIR=/var/lib/cnc-dnc/programs

# Backend NATS endpoint reachable by the Pi
NATS_URL=nats://<backend-ip>:4222
NATS_STREAM=DNC_PROGRESS

# Identity of this Pi/CNC for progress subjects (DNC_PROGRESS.<MACHINE_ID>)
MACHINE_ID=<MACHINE_ID>

# Path to the Heidenhain sender script on the Pi
HEIDENHAIN_SENDER=/home/pi/heidenhain_sender.py

# Web server binding on the Pi
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8083

# Logging
LOG_LEVEL=info

